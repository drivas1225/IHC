using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TheSongManager : MonoBehaviour
{
    static int music;
    // Start is called before the first frame update
    void Start()
    {
        music = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
